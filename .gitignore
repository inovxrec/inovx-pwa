# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Edge function secrets — the VAPID private key signs every push and must never
# be committed. Regenerate with scripts/generate-vapid-keys.mjs if it leaks.
supabase/functions/.env
supabase/.env

# Issued passwords, written once by scripts/provision-roster.mjs and meant to be
# handed out and deleted. Never committed.
roster-passwords.csv
*-passwords.csv
