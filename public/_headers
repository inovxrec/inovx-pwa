# Hashed filenames change whenever their content does, so they can be kept
# forever. The shell and the service worker must not be, or a deploy would not
# reach anyone still holding the old one.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/index.html
  Cache-Control: no-cache

/sw.js
  Cache-Control: no-cache
